const todo = {
    id: 'asdfpoijwermasdf',
    text: 'Pay the bills',
    completed: false
}

// const text = todo.text
// const completed = todo.completed

const { text, completed } = todo

console.log(text)
console.log(completed)
