const myAge = 27
const message = myAge >= 18 ? 'You can vote!' : 'You cannot vote.'

/*
if (myAge >= 18) {
    message = 'You can vote!'
} else {
    message = 'You cannot vote.'
}
*/

console.log(message)
